apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: {{ .Values.ai.name }}-monitor

  labels:
    release: monitoring

spec:

  selector:
    matchLabels:
      app: {{ .Values.ai.name }}

  endpoints:
    - port: http
      path: /metrics
      interval: 15s