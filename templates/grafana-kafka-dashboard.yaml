apiVersion: v1
kind: ConfigMap

metadata:
  name: kafka-dashboard

  labels:
    grafana_dashboard: "3"

data:
  kafka-dashboard.json: |
{{ .Files.Get "monitoring/dashboards/kafka-dashboard.json" | indent 4 }}